---
layout: base
title: "Projects"
permalink: /projects/
---

<div id="smalltopnavback"></div>

<div class="container marketing">
    <div class="row">
        <div class="col-lg-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Projects</li>
                </ol>
            </nav>
        </div>
    </div>
    
    <div class="row">
        <div class="col-lg-12">
            <h1>Projects</h1>
            <p class="lead">Engineering projects, build logs, and technical implementations.</p>
            
            {% set postslist = collections.projects | reverse %}
            {% if postslist.length > 0 %}
                {% for post in postslist %}
                    <article class="blog-post-preview">
                        <div class="row">
                            <div class="col-lg-12">
                                <h3><a href="{{ post.url }}" class="text-decoration-none">{{ post.data.title }}</a></h3>
                                <p class="post-meta">
                                    <small><i>{{ post.data.author or "Netopya" }} on {{ post.data.date | dateFormat }}</i></small>
                                    {% if post.data.tag %}
                                    <span class="badge ms-2 {% if post.data.tag == 'Project' %}bg-primary{% elif post.data.tag == 'Tutorial' %}bg-success{% elif post.data.tag == 'Tool' %}bg-info{% else %}bg-secondary{% endif %}">{{ post.data.tag }}</span>
                                    {% endif %}
                                </p>
                                
                                {% if post.data.overfold_content %}
                                    <div class="article-preview">
                                        {{ post.data.overfold_content | safe }}
                                    </div>
                                {% else %}
                                    <div class="article-preview">
                                        {{ post.templateContent | excerpt(200) }}
                                    </div>
                                {% endif %}
                                
                                <p><a href="{{ post.url }}" class="btn btn-primary">View Project →</a></p>
                                <hr>
                            </div>
                        </div>
                    </article>
                {% endfor %}
            {% else %}
                <p>No projects available yet. Check back soon!</p>
            {% endif %}
        </div>
    </div>
</div> 